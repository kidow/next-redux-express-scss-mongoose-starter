const nextRoutes = require('next-routes')
const routes = (module.exports = nextRoutes())

// Custom Routing With Parameters
// routes.add('tag', '/tag/:tag')
// routes.add('post', '/post/:id')
// routes.add('editor', '/editor/:id')
// routes.add('login')
